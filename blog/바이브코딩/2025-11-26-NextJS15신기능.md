---
title: Next.js 15 신기능 소개
tags: [Next.js, React, 웹개발]
category: 바이브코딩
summary: Next.js 15의 새로운 기능들을 살펴봅니다.
date: 2025-11-26
---

# Next.js 15 신기능 소개

Next.js 15에 추가된 새로운 기능들을 알아봅시다!

## 주요 신기능

### 1. Turbopack (베타)
더 빠른 번들링을 위한 새로운 빌드 도구입니다.

```bash
npm run dev --turbo
```

### 2. Server Actions 개선
서버 사이드 로직을 더 쉽게 작성할 수 있습니다.

```typescript
'use server'

async function submitForm(formData: FormData) {
  const name = formData.get('name')
  // 서버에서 실행되는 코드
  await saveToDatabase(name)
}
```

### 3. 향상된 이미지 최적화
`next/image` 컴포넌트의 성능이 더욱 개선되었습니다.

```tsx
import Image from 'next/image'

export default function Page() {
  return (
    <Image
      src="/profile.jpg"
      alt="Profile"
      width={500}
      height={500}
      priority
    />
  )
}
```

## 마이그레이션 팁

기존 프로젝트를 업그레이드할 때 주의할 점:
1. ✅ package.json에서 Next.js 버전 업데이트
2. ✅ Breaking changes 확인
3. ✅ 테스트 실행

Next.js로 더 나은 웹을 만들어봅시다! 🚀
